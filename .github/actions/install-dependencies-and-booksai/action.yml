name: "Install-dependencies-and-booksai"
description: "Install booksai and its dependencies"
runs:
  using: "composite"
  steps:
    - name: Install-dependencies
      run: |
        python -m pip install --upgrade pip setuptools wheel
        pip install -e .[ci]
      shell: bash
